---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: protota
  namespace: default
  labels:
    app: protota
spec:
  replicas: 1
  selector:
    matchLabels:
      app: protota
  template:
    metadata:
      labels:
        app: protota
    spec:
      containers:
        - name: web
          image: docker.io/library/protota:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: 100m
              memory: 64Mi
            requests:
              cpu: 10m
              memory: 32Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
      securityContext:
        runAsNonRoot: true
        runAsUser: 101
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: protota-svc
  namespace: default
spec:
  selector:
    app: protota
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: protota
  namespace: default
spec:
  defaultBackend:
    service:
      name: protota-svc
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - protota
