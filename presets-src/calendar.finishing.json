{
  "title": "GNOME Calendar",
  "screens": [
    { "id": "month", "entry": "gui/gcal-window.ui", "title": "Month", "width": 768, "height": 600,
      "runtimeProbe": true,
      "overrides": [
        { "id": "views_stack", "set": { "visibleChildName": "month" },
          "probeEvidence": { "probeVersion": 2, "buildableId": "views_stack",
                             "expect": { "visibleChildName": "month", "mapped": true } },
          "why": "Native probe (2026-08-02): views_stack shows the month page in the default GSettings active-view; its settled stock-widget descendants are projected from the same frame." }
      ] },
    { "id": "week", "entry": "gui/gcal-window.ui", "title": "Week", "width": 768, "height": 600,
      "runtimeProbe": true,
      "probeFile": "calendar-week.probe.json",
      "overrides": [
        { "id": "views_stack", "set": { "visibleChildName": "week" },
          "probeEvidence": { "probeVersion": 2, "buildableId": "views_stack",
                             "expect": { "visibleChildName": "week", "mapped": true } },
          "why": "Native probe (2026-08-02): active-view=week selects this page; its settled stock-widget descendants are projected from the same frame." }
      ] }
  ],
  "edges": [
    { "from": "month", "to": "week", "why": "View switcher → Week" }
  ]
}
